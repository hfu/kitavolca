# Schema Definition

**実データ（樽前山 VBM/VLCM）に基づき確定。** 測定コマンドと結果は各節に記載。

## VBM Schema

### 元属性（Shapefile → GeoJSONSeq 変換直後）

`分類コード` の値によって、属性の組み合わせは以下の9パターンに分かれる（`work/vbm/all_raw.ndjson` を実測）。

| 属性キーの組み合わせ | ジオメトリ型 | 代表 `分類コード` |
|---|---|---|
| ID番号, 出典コード, 出典レベル, 分類コード, 注記, 表示区分 | LineString | 1102, 1103, 2101, 2103, 2106, 2107, 2301, 2306, 2311, 5101, 5102, 5106, 5202, 5211, 5226-5239, 6201 |
| ID番号, 出典コード, 出典レベル, 分類コード, 注記 | Polygon | 3001-3004 |
| ID番号, 出典コード, 出典レベル, 分類コード, 注記, 表示区分 | Polygon | 5112 |
| ID番号, 出典コード, 分類コード, 名称 | MultiPoint | 3581, 3582, 4257, 6221, 6222, 6241, 6242 |
| ID番号, 出典コード, 分類コード, 注記 | LineString | 4265 |
| ID番号, 出典コード, 分類コード, 名称, 表示区分 | LineString | 5265 |
| ID番号, 出典コード, 分類コード, 標高, 表示区分 | LineString | 7101, 7102, 7105, 7106（等高線） |
| ID番号, 出典コード, 出典レベル, 分類コード, 水深, 表示区分 | LineString | 7132, 7133, 7134（等深線） |
| ID番号, 出典コード, 分類コード, 水深値 | MultiPoint | 7135 |
| ID番号, 三角点標高, 出典コード, 分類コード, 水準点標高, 表示区分 | MultiPoint | 7301-7308（三角点・水準点） |
| ID番号, 出典コード, 分類コード, 注記 | MultiPoint | 8110-8171（注記点） |

測定コマンド:
```bash
jq -r '[(.properties["分類コード"]|tostring), .geometry.type, (.properties|keys|sort|join("|"))] | @tsv' work/vbm/all_raw.ndjson | sort -u
```

### 2026-07-04 追記: 分類コード → 由来シェープファイル名（実際の意味）の対応

これまで `分類コード` の実際の意味（何を表す図式か）を特定できていなかったが、樽前山の ZIP 内シェープファイルを1つずつ変換して `分類コード` を確認し、直接対応付けた。

測定コマンド:
```bash
gdal vsi list -R -f json "/vsizip/$(pwd)/src/tarumae_vbm.zip" | jq -r '.[] | select(test("\\.shp$"; "i"))' | while read -r shp; do
  name=$(basename "$shp" .shp)
  codes=$(ogr2ogr --config SHAPE_ENCODING CP932 -f GeoJSONSeq /vsistdout/ "/vsizip/$(pwd)/src/tarumae_vbm.zip/$shp" 2>/dev/null \
    | jq -r '.properties["分類コード"]' | sort -u | tr '\n' ',')
  echo "${name}: ${codes}"
done
```

結果（樽前山データで確認。他火山でも同じ対応と推定されるが未検証）:

| シェープファイル名 | 分類コード | 意味 |
|---|---|---|
| ヘリ離着陸場 | 6241 | ヘリ離着陸場 |
| 区域界 | 6201 | 区域界 |
| 噴火口・噴気口 | 6221 | 噴火口・噴気口 |
| 基準点 | 7301, 7302, 7303, 7308 | 三角点・水準点 |
| 建物 | 3001, 3002, 3003, 3004 | 建物（Polygon。件数最多、8火山結合で205,779件） |
| 水域 | 5112 | 水域（Polygon） |
| 水涯線・海岸線 | 5101, 5102, 5106 | **海岸線・水涯線**（8火山結合で5101=20,564件, 5102=11,800件。北海道全域の海岸線を含むため高密度） |
| 水深値 | 7135 | 水深値点 |
| 水部構造物 | 5202, 5211, 5226-5239 | 水部構造物（堤防等） |
| 注記 | 8110-8171 | 注記点 |
| 温泉 | 6222 | 温泉 |
| 火山観測施設・機器 | 4257 | 火山観測施設・機器 |
| 砂防ダム・治山ダム | 5265 | 砂防ダム・治山ダム |
| 等深線 | 7132, 7133, 7134 | 等深線 |
| 等高線 | 7101, 7102, 7105, 7106 | 等高線（8火山結合で7102=135,632件, 7101=31,811件。minzoom=12を適用済み） |
| 行政界 | 1102, 1103 | 行政界 |
| 送電線 | 4265 | 送電線 |
| 道路 | 2101, 2103, 2106, 2107 | **道路**（8火山結合で2101=88,615件, 2106=20,114件。北海道全域の道路網を含むため高密度） |
| 避難所等 | 3581, 3582, 6242 | 避難所等 |
| 鉄道 | 2301, 2306, 2311 | 鉄道 |

**低ズームでのタイルサイズ超過との関係**: `docs/zoom-policy.md` に記載の通り、大雪山の等高線問題を修正した後もタイルサイズ警告が残った原因は、`道路`(2101/2106)・`水涯線・海岸線`(5101/5102)・`建物`(3001) が北海道全域にわたって高密度に存在し、かつ現状これらに `minzoom` 制限が一切無いため（`-Z 5` の全体設定のままズーム5から表示される）。これらは火山ごとの局所的な地物ではなく、北海道の基盤地図として全域に存在するため、火山を追加結合するたびに単調に密度が増していく。等高線と同様の `minzoom` シフトが有効な可能性が高いが、値の決定にはユーザーによる目視確認が必要（次のアクション待ち）。

### 保持する属性（確定）

`scripts/build-vbm.sh` の実装通り、以下を採用する。

- **削除**: `ID番号`（feature 内シーケンス番号。スタイリング・クエリに無価値なため削除）
- **保持（そのまま）**: `出典コード`, `出典レベル`, `標高`, `水深`, `水深値`, `名称`, `注記`, `表示区分`, `三角点標高`, `水準点標高`, `分類コード`
  - いずれも小さいスカラー値で、タイルサイズへの影響は軽微
  - `分類コード` は `properties` 内にも残したまま、`tippecanoe.layer`（文字列化）としても複製する。フィルタ用に文字列比較しやすくするため
- **追加**: feature 直下の `tippecanoe.layer`（`分類コード` の文字列版）。`tippecanoe.minzoom = 11` を等高線・等深線系コードにのみ付与

### 分類コードの実測分布（樽前山データ、全 201,939 features）

上位のみ抜粋（全件は `jq -r '.tippecanoe.layer' work/vbm/vbm_filtered.ndjson | sort | uniq -c | sort -rn` で再現可能）:

| 分類コード | 件数 | 備考 |
|---|---|---|
| 3001 | 112,444 | Polygon、最多 |
| 2101 | 29,863 | LineString |
| 7102 | 16,837 | 等高線、minzoom=11 適用 |
| 2106 | 15,175 | LineString |
| 5101 | 5,685 | LineString |
| 7106 | 824 | 等高線、minzoom=11 適用 |
| 7133 | 13 | 等深線、minzoom=11 適用 |
| 7135 | 1 | 水深値点、minzoom=11 適用 |

### 2026-07-04 追記: minzoom を全等高線・等深線系コードで 12 に統一（`--drop-densest-as-needed` 廃止）

北海道8火山を結合した際、大雪山エリアで等高線（`7101`/`7102`）が大量にドロップされる問題が目視で見つかった（`--drop-densest-as-needed` によるもの）。原因を `tippecanoe-decode` で分析したところ、該当タイルの内容は `7102`(主曲線)・`7101`(計曲線) がバイト数の大半を占めていることを確認。

対策として `--drop-densest-as-needed` を完全に廃止し、代わりに等高線・等深線系8コード全て（`7101/7102/7105/7106/7132/7133/7134/7135`）の `tippecanoe.minzoom` を統一した（旧: `7102/7106/7133/7135` のみ `minzoom=11`、`7101/7105/7132/7134` は無制限という不整合な設計だった）。これによりデータの間引き・欠損は一切発生しない（feature数は549,748件のまま不変）。

結果: 大雪山エリアの警告タイル（z11で7件）は全て解消。樽前山エリアには別原因（後述）で4タイルの警告が残るが、等高線とは無関係と判明した——この残りの問題は道路・建物コードの正体特定（後述「2101/2106/5101/5102の高密度」節）と合わせて解決し、**最終的に minzoom は等高線・道路・建物の計16コードで 13 に統一**された（経緯は `docs/zoom-policy.md` 参照）。

これにより、下記「未解決事項（水深系）」の対象コード分け自体は設計判断として維持しつつ、**minzoom は8コード共通で13** という運用に変更された。以下の分析（計曲線/主曲線のペア構造の実測根拠）自体は変更なし、参考情報として残す。

### 等高線・等深線コードの minzoom=11 対象選定の根拠（実測により確定。2026-07-04 時点で minzoom の値自体は13に変更されたが、コード間のペア構造分析は有効）

`7101/7102`, `7105/7106` は同じ属性形状（標高を持つ LineString）だが、`分類コード` ごとの標高値の分布を実測すると、明確な「計曲線（強調線・間引かれた線）/ 主曲線（通常線・密な線）」のペア構造になっていることがわかった。

測定コマンド:
```bash
jq -r 'select(.properties["分類コード"]==<code>) | .properties["標高"]' work/vbm/all_raw.ndjson \
  | awk '{tot++; if ($1 % 25 == 0) m25++} END{print m25"/"tot}'
```

結果:

| 分類コード | 件数 | 標高が25の倍数の割合 | 解釈 |
|---|---|---|---|
| 7101 | 3,723 | 3722/3723 (99.97%) | **計曲線**（25m間隔の強調線） |
| 7102 | 16,837 | 1/16,837 (0.01%) | **主曲線**（5m間隔。7101と重複する25の倍数を意図的に除外） |
| 7105 | 90 | 80/90 (88.9%) | 補助曲線系の計曲線 |
| 7106 | 824 | 3/824 (0.36%) | 補助曲線系の主曲線 |

7102 は7101（25の倍数）をほぼ完全に除外しており（16,837件中1件のみ例外）、7101と7102が「同じ地形を、間引いた強調表示用（計曲線）」と「全間隔表示用（主曲線）」に意図的に分割していることが確認できる。7105/7106も同型のペア。

**結論**: `minzoom=11` が付与されている `7102, 7106` は各ペアの「主曲線（密度が高く、低ズームでは線が密集しすぎる）」側であり、`7101, 7105`（計曲線・間引き済みで密度が低い）は同じ理由でのズーム制限が不要なため対象外——という設計は密度データと整合しており、見落としではなく合理的な選択だったと判断できる。

### 未解決事項（水深系: 7132/7133/7134）

等深線側にも同様の計曲線/主曲線ペア構造がある可能性が高いが（`7133` のみ `minzoom=11` 対象）、樽前山データでは該当件数が極端に少なく（7132: 9件, 7133: 13件, 7134: 3件）、標高系と同じ手法で統計的に裏付けるには不十分。水深値も 0.25/0.5/0.63/0.88 とバラつきがあり、単純な倍数関係では説明しきれない。

- 海岸線・湖沼を含む火山（例: 支笏湖に近いエリア、有珠山の噴火湾側など）のデータで再測定し、同じ「間引き倍数関係」が成立するか確認すること
- 2026-07-04 時点で minzoom は8コード共通で13（上記追記を参照）。コード分けの倍数関係の検証自体は引き続き未解決

**2026-07-04 追記**: `just fetch-vbm usu`（有珠山、噴火湾に面する）で実際に検証を試みたが、有珠山の VBM Shapefile ZIP（`vbm07-2022-shp.zip`）には `等高線.shp` / `等深線.shp` に相当するファイル自体が含まれておらず（17ファイル中に無し）、標高・水深系コード（7101-7106, 7132-7135）を1件も含まないことが判明した。海岸に近い火山だからといって等深線データが含まれているとは限らない（マップシートの版・範囲による）。水深系コードの検証には、実際に等深線を含むことを事前に確認した火山データが必要——単に「海に近い」だけでは不十分。この件は未解決のまま持ち越す。

### 分類コード 2101/2106/5101/5102 の正体特定と対応（2026-07-04 解決）

大雪山の等高線問題を修正した後、樽前山エリアに残った4件のタイル警告（z8/10/11/12）を `tippecanoe-decode` で調べたところ、等高線とは無関係で、`2101`・`2106`・`5101`・`5102` が北海道全域にわたって非常に高密度に存在していることが原因と判明した。例えばズーム8のタイルでは `2101` だけで36,677件・6.8MB相当（decoded JSON換算）に達する。

正体は、樽前山のシェープファイルを1つずつ変換して `分類コード` を直接確認して判明した（詳細な対応表は上記「分類コード → 由来シェープファイル名」参照）:
- `2101/2103/2106/2107` = **道路**
- `5101/5102/5106` = **水涯線・海岸線**

ユーザーの指示で[国土地理院最適化ベクトルタイル](https://github.com/gsi-cyberjapan/optimal_bvmap)の設計（`style/std.json`）を参考にした。GSIでは主要道路がズーム4-10、詳細な道路はズーム11から、建物は面積による段階的な間引きを行っているが、**今回は建物の面積間引きのような作り込みは行わない方針**とし、道路・建物にも等高線と同じ単純な `minzoom` シフトを適用した。海岸線はGSIの設計でも明示的な制限が無いため、意図的に `minzoom` を付与しなかった。

最終結果（`docs/zoom-policy.md` に詳細）: 等高線・等深線系8コード + 道路4コード（`2101/2103/2106/2107`）+ 建物4コード（`3001/3002/3003/3004`）の計16コードを `minzoom=13` に統一。北海道8火山結合データで、タイルサイズ超過（非圧縮バイト数で実測）はゼロになることを確認済み。

## VLCM Schema

### 元属性

`work/vlcm/natural.ndjson`, `work/vlcm/artificial.ndjson` を実測（`jq -r '.properties | keys | join(",")' | sort -u`）。

- `ID`, `code1`, `code2`, `code3`, `code4`, `name` が基本セット（2024年版までの5火山で共通、VBM のような分岐はない）
- `code1`〜`code4` は階層的な分類コード（桁数が増えるほど詳細）。例: `code1=1, code2=101, code3=10101, code4=1010101, name=火口跡`
- `name` はすでに日本語の人間可読ラベル（VBM の数値 `分類コード` と異なり、別途コード表を参照しなくても意味がわかる）
- **例外（恵山、2026年版）**: 下記「2026-07-16 追記」参照。基本セットに加えて `code`, `code5`, `code6`, `class1`〜`class6` を持つ、より詳細な新スキーマ

### 保持する属性（確定）

- **全属性をそのまま保持**（フィルタ不要）。属性数が少なく（6キー固定）、`name` が既に人間可読なため、削除・変換の必要性がない

### natural / artificial の実測件数（樽前山データ）

| source-layer | 件数 | ジオメトリ内訳 |
|---|---|---|
| natural | 1,581 | Polygon 1,556 / LineString 25 |
| artificial | 13 | Polygon 13（全件 `code2=105`, `name=人工改変地`） |

`artificial` が極端に少ないのは樽前山エリアの人工改変地が実際に少ないためで、パイプラインの不具合ではない（ファイル名 `shizen`/`jinko` によるレイヤ振り分けは正しく機能している）。

### 2026-07-05 追記: VLCM の公式配色を調査し `docs/style.json` に反映

火山土地条件図の色は、国土地理院防災ポータルの凡例ページ（https://disaportal.gsi.go.jp/maps/image/legend/vlcd.html）から火山ごとの凡例画像（例: 樽前山 `https://maps.gsi.go.jp/legend/l_vlcd_10tarm.jpg`）をダウンロードし、Python（Pillow）でスウォッチ部分のピクセルを平均してRGB値を実測した。

**重要な発見**: 色は `code2`/`code3`（分類コード階層）単位ではなく、**`name`（地形名）単位で個別に割り当てられている**。例えば `code3=10101`（火口グループ）に属する「カルデラ壁」「火口原」「火口壁」は、同じ code3 でありながら全く異なる色（水色・クリーム・ピンク）が使われている。そのため、`docs/style.json` では `name` 属性に対する `match` 式で配色し、名称ごとの色を1つずつ実測値で埋めた（火口跡・火口・階段状地形など、凡例上ライン記号のみで塗り色を持たないものは `fill-opacity: 0` + 赤系のラインとして再現）。

「1739年火砕流堆積地形」のような噴火年を含む地名は樽前山・支笏火山に固有で、他火山では別の名称になる。有珠山のデータ（`just fetch-vlcm usu`）で実際に確認したところ、樽前山の凡例に無い名称（「馬蹄形カルデラ形成以前の火山体斜面」「潜在円頂丘」「スコリア丘」等）が多数存在した。これらは樽前山凡例の類似カテゴリ（成層火山体斜面・溶岩円頂丘・火砕丘等）から類推した色を暫定的に割り当てている（`docs/style.json` 生成に使ったスクリプトの `NAME_COLORS` コメント参照）。未知の `name` に対しては `code2` ベースのフォールバック色（101=火山地形系, 102=基盤山地, 103=堆積地形, 104=断層, 106=水部・ガリ）を用意し、さらに未知の場合は灰色にフォールバックする。

今後別の火山を追加した際、樽前山の凡例に無い `name` が出てきたら、その火山自身の公式凡例画像（`disaportal.gsi.go.jp` のページから辿れる）を確認し、実測値で `docs/style.json` の `NAME_COLORS` 相当のテーブルを更新すること。

**2026-07-05 追記（ポリゴン境界線の低ズーム対策）**: 実データで確認したところ、`名称="火口"` は（他の火口跡・階段状地形等と違い）実際には Polygon ジオメトリだった。低ズームでは1,500件超のPolygon境界線が重なって黒く潰れて見える問題があったため、`vlcm-natural-line` を3レイヤーに分割した:
- `vlcm-natural-crater-outline`: `火口`（Polygon）専用、赤線、ズーム制限なし
- `vlcm-natural-boundary`: その他の Polygon 境界線、`minzoom: 11`（低ズームでは非表示にして黒つぶれを回避）
- `vlcm-natural-symbol-line`: 実際に LineString 型の地形記号（火口跡・階段状地形・断層等）、赤/黒線、ズーム制限なし

**2026-07-05 追記（VLCM 全6火山への対応）**: `just fetch-vlcm` で対応済みの全6火山（雌阿寒岳・十勝岳・樽前山・有珠山・北海道駒ヶ岳・恵山）を取得・結合したところ、`name` の種類は191種に増え、樽前山の凡例で色が判明しているのはそのうち45種のみだった（残り146種は`code2`ベースのフォールバック色）。各火山の地形名は火山ごとの噴火史・地形に強く紐づいた固有名（例:「中マチネシリ」「駒ヶ岳岩屑なだれ堆積地」「恵山溶岩円頂丘」）が大半で、樽前山・有珠山からの類推だけではカバーしきれない。

Playwrightで雌阿寒岳・十勝岳・北海道駒ヶ岳・恵山の4火山を目視確認し、**フォールバック色で破綻はしていない**（火口ポリゴンの赤枠、code2別の色分けは機能）ことを確認したが、公式の凡例に基づく正確な配色ではない。恵山はVBM Shapefileが無いため等高線等は表示されない（VLCMのみ表示、想定通り）。

精密な配色にするには、各火山の凡例画像（`disaportal.gsi.go.jp` 経由）を追加で取得・実測する必要がある。次のアクションとして残す。

**2026-07-05 追記（雌阿寒岳・十勝岳・北海道駒ヶ岳の凡例調査）**: 上記3火山の公式凡例画像を取得し、57種類の地形名について色を追加した（`docs/style.json` 生成に使ったスクリプト `add_more_colors.py` の `NEW_NAME_COLORS` 参照）。

- **手法の効率化**: 手作業でのピクセル座標特定が非効率だったため、`scipy.ndimage.label` による連結成分分析でスウォッチ矩形を自動検出するスクリプト（`detect_swatches.py`）を作成。「背景（白）でも、文字（低彩度）でもない領域」をマスクし、幅20px・高さ10px以上の連結領域だけをスウォッチとして抽出、中心60%領域の平均色を採用する方式。これにより3火山分の色抽出が大幅に高速化した
- **名称の重複解決方針**: 同じ地形名（例:「火砕丘」）が複数火山の凡例で異なる色を持つ場合がある（GSIの配色は本来「火山ごと」に閉じた凡例だが、kitavolcaは全火山を1つのタイルセットに統合しているため）。この場合は**先に登録された色（樽前山・有珠山由来）を優先し、上書きしない**方針とした。雄阿寒岳関連の地形名は実データ上「（雄阿寒岳）」という接頭辞が付いており、雌阿寒岳本体の同名地形とは文字列として別物になるため、実際の衝突は限定的だった
- **信頼度**: 雌阿寒岳・十勝岳は凡例画像が横長で列ごとの構造が明瞭だったため高精度。北海道駒ヶ岳は単一の縦長画像で行間隔が詰まっており、自動検出結果と地形名の対応付けの確信度がやや低い（今後、目視で再確認する余地あり）
- **恵山**: 引き続き凡例画像が見つからず、`code2` フォールバックのまま

**2026-07-06 追記（恵山の配色を他火山からの類推で確定）**: 恵山のデータを調べたところ、`code2`/`code3` が他火山と全く異なる番号体系（`01/02/50/51/52/53/99`。他火山は`101/102/103/104/106`）であることが判明し、既存の`code2`フォールバックも機能していなかった（未定義コードは最終フォールバックの灰色一色になっていた）。

ユーザー指示により、公式凡例の代わりに**既存の火山（樽前山・有珠山・雌阿寒岳・十勝岳・北海道駒ヶ岳）で判明している色を地形の概念（火口壁・溶岩円頂丘・火砕流堆積地・浸食地形・堆積地形等）ごとに類推**して41種の`name`に色を割り当てた（`add_esan_colors.py`の`ESAN_NAME_COLORS`）。ジオメトリ型を確認したところ「火口・カルデラ状地形縁」等の縁線はLineStringだったため、他火山の「火口」と同様にfill非表示＋赤線のシンボル扱いとした（`ESAN_LINE_ONLY`）。

Playwrightで確認したところ、単調な灰色一色から、ドーム地形（紫系）・火砕流堆積地（サーモン系）・侵食地形（緑灰系）等に色が分かれ、視覚的な情報量が大幅に向上した。ただし実測ではなく類推による色のため、実際にGSIが恵山の凡例を公開した際は差し替えが必要。

また、`natural` レイヤーには Polygon/LineString 以外に **Point**（`噴気孔`, `泥火山`。十勝岳のデータで新規発見）も存在することが分かり、`vlcm-natural-point`（circle レイヤー）を追加した。

### 2026-07-16 追記: 恵山（2026年版・新スキーマ）の属性を追加保持

測量成果使用承認取得後の再生成（`just build-vlcm`）で `just validate` が失敗し、原因を調べたところ `esan_vlcm.zip`（ファイル名 `vlcm_shp-09esn-2026`）が他火山（2024年版）と異なる、より詳細な属性スキーマを持つことが判明した。GSI が恵山を新スキーマで公開し直した、いわゆる「ニュープロダクト」である。

- 従来（2024年版・5火山共通）: `ID`, `code1`, `code2`, `code3`, `code4`, `name`（6キー）
- 恵山（2026年版）: 上記に加えて `code`（`code1`〜`code6` を連結した12桁の完全パスコードとみられる。例 `101000100100`）, `code5`, `code6`, `class1`〜`class6`（`name` に至るまでの分類階層を段階ごとに文字列化したもの）

サンプル（`work/vlcm/natural_0.ndjson` より）:
```json
{
  "ID": 1,
  "code1": "1", "code2": "01", "code3": "00", "code4": "01", "code5": "001", "code6": "00",
  "code": "101000100100",
  "class1": "火山地形", "class2": "火口・カルデラ", "class3": null,
  "class4": "火口・カルデラ地形", "class5": "火口・カルデラ縁", "class6": null,
  "name": "火口・カルデラ縁"
}
```

**対応方針**: 新属性はそのまま `dst/vlcm.pmtiles` に保持する（`build-vlcm.sh` に属性フィルタは元々無く、`ogr2ogr` の出力をそのまま tippecanoe に渡している）。`Justfile` の `validate` タスクの許可属性リスト（`known`）に `code`, `code5`, `code6`, `class1`〜`class6` を追加して対応した。

**`docs/style.json` への影響は無し**: VLCM の配色は Polygon/LineString とも `name` 属性（地形名の文字列）への `match` 式で決まっており（フォールバックのみ `code2` を参照、[docs/style.json:1421](../docs/style.json)）、`class1`〜`class6`・`code`・`code5`・`code6` はどこからも参照されない。恵山の `name` 値47種のうち46種は既存の類推配色（上記「2026-07-06 追記」参照）でカバー済み。唯一未登録の `地形界`（802件、すべて `LineString`）はデフォルトのフォールバック配色（`rgba(0,0,0,0.4)`）で描画され、破綻はない。

**今後**: 他火山も GSI 側で新スキーマに更新された場合、同様に `class1`〜`class6` 等が出現する可能性がある。その際は本節と同じ手順（`known` リスト更新、`name` の新規値がスタイルに未登録でないか確認）で対応する。

### 2026-07-17 追記: VBM点地物の分類コード ↔ bvmap地図記号の対応

[Issue #2](https://github.com/hfu/kitavolca/issues/2) の指示により、VBMの点地物（分類コードあり）と、背景に使っている国土地理院最適化ベクトルタイル（bvmap）が読み込んでいるsprite（`https://gsi-cyberjapan.github.io/optimal_bvmap/sprite/std.json`）の地図記号を突き合わせた。

sprite内のアイコン名一覧を実際に取得し、VBMの点系分類コード（`3581, 3582, 4257, 6221, 6222, 6241, 6242, 7301-7303, 7308`）と照合した結果:

| 分類コード | 意味 | 対応する地図記号 | 備考 |
|---|---|---|---|
| 6221 | 噴火口・噴気口 | `噴火口・噴気口-20` | 完全一致 |
| 6222 | 温泉 | `温泉-20` | 完全一致 |
| 7301, 7302, 7303, 7308 | 三角点・水準点 | `三角点-20` / `水準点` | **コード単位ではなくフィーチャー単位**で判定が必要（後述） |
| 6241 | ヘリ離着陸場 | なし | sprite中に該当記号なし。円マーカーのまま |
| 3581, 3582, 6242 | 避難所等 | なし | 同上 |
| 4257 | 火山観測施設・機器 | なし | 同上 |

**三角点/水準点の判定がコード単位でない**: `7301`等の分類コードは「三角点・水準点」codeとしてひとまとめに割り当てられているが、実データ（樽前山ほか結合済み `work/vbm/vbm_filtered.ndjson`）を実測すると、同じコード内で `三角点標高`/`水準点標高` のどちらが非nullかが混在している（例: `7301` 内訳は「三角点標高のみ」221件・「両方あり」187件）。そのため `docs/style.json` の `vbm-control-7301/7302/7303/7308` では、分類コードではなく `["case", ["!=", ["get","三角点標高"], null], "三角点-20", "水準点"]` という属性ベースの `icon-image` 式で振り分けている（両方non-nullの場合は三角点を優先）。

**実装**: `vbm-point-6221`/`vbm-point-6222`/`vbm-control-7301,7302,7303,7308` を `type:"circle"` から `type:"symbol"`（`icon-image` + `icon-size:0.8` + `icon-allow-overlap:true`）に変更。対応記号が無い6241/3581/3582/6242/4257は従来の円マーカーのまま。sprite自体は既存の `docs/style.json` の `sprite` キーで読み込み済みのため、追加のアセット取得は不要。PMTiles再生成も不要（スタイルのみの変更）。

### 2026-07-17 追記: 等高線の計曲線・主曲線でminzoomを分離

これまで等高線系4コード（`7101`計曲線25m間隔・`7102`主曲線5m間隔・`7105`/`7106`同型の補助曲線ペア）は一律 `tippecanoe.minzoom=13` だったため、z12以下では等高線が一切表示されず、火山基本図の特徴である詳細な等高線情報が低ズームで完全に失われていた。

対策として、密度の低い計曲線（`7101`/`7105`）だけを低ズームから含め、密度の高い主曲線（`7102`/`7106`）は従来通り `minzoom=13` に据え置いた。

当初 bvmap 自身の等高線表示開始ズーム（z9）に合わせて `tippecanoe.minzoom=9` を試したが、8火山・全ズームでの実測（`pmtiles tile dst/vbm.pmtiles <z> <x> <y> | gunzip -c | wc -c`、各火山中心タイル＋3x3近傍）で、十勝岳z10（721,347 bytes）・十勝岳z9（622,743 bytes）・樽前山z10（520,166 bytes）の3タイルが500KB（tippecanoeの警告閾値、非圧縮）を超過することが判明（十勝岳は計曲線データが特に密）。`docs/schema.md` に記録済みの過去の教訓（「tippecanoeのビルドログが警告0件でも実際には超過しているケースがある」）通り、今回もビルドログ自体には警告が出ていなかった。

`minzoom=11` まで段階的に引き上げて再実測したところ、8火山×z9-11×3x3近傍（135タイル）全てが500KB未満（最大は十勝岳z11の315,735 bytes）となったため、この値で確定した。

`docs/style.json` 側は、計曲線レイヤーの `line-width` を `["step", ["zoom"], 0.5, 13, 1.2]` に変更（z<13では主曲線と同じ太さ0.5で描画し、計曲線だけが浮いて見える「雑さ」を回避。z>=13で主曲線が合流したら、従来通りの強調表示である太さ1.2に戻る）。

### 2026-07-17 追記2: z9〜z10にも「標高100mおき」の間引き計曲線を追加

ユーザーから「z8〜z10でも等高線をある程度残せないか」と問われ、追加検証した。

まず `minzoom=10` を試したが、z10で従来と全く同じ4タイルが500KB超過（十勝岳・大雪山721,347 bytes、樽前山・倶多楽520,166 bytes）した。原因は tippecanoe の自動線簡略化では対処しきれない**フィーチャー数自体の多さ**（十勝岳エリアの計曲線本数が突出）であり、単純に `minzoom` を下げるだけでは解決しないことが判明。

そこで、計曲線をさらに間引いた広域概観用のサブセットを別途用意する方針に切り替えた。等高線・道路・建物の分類でも使った「標高の倍数」による選別と同じ発想で、計曲線のうち**標高が100mの倍数のもの（全計曲線32,299件中7,146件、22.1%）だけを z9 から表示**し、それ以外（78%）は従来通り z11 から表示する2階層構成にした（`scripts/build-vbm.sh` で `.tippecanoe.minzoom` を `標高 % 100 == 0 ? 9 : 11` の三項式で分岐）。

実測結果（8火山×z8-10×3x3近傍、72タイル/ズーム）:

| ズーム | 内容 | 最大タイルサイズ |
|---|---|---|
| z8 | 等高線データなし（間引き版でも、南北海道の複数火山が1タイルに収まる広域タイルでは合算が500KBを超えるため断念） | 472,270 bytes |
| z9〜z10 | 標高100mの倍数の計曲線のみ | 350,518 bytes |
| z11〜z12 | 計曲線全部（25m間隔） | 315,735 bytes |

z8だけは、間引き後の100mおき計曲線を各火山の複数が1タイルに合算されるほど広域なため518,251 bytesとわずかに超過し、断念した（`minzoom` を9→8にしただけでは通らず、間引き開始ズーム自体をz9に留めることで解決）。

`docs/style.json` の `vbm-contour-7101`/`vbm-contour-7105` の `minzoom` は9に変更。線の太さはz9〜z10の間引き表示でも既存の`line-width`式（`["step",["zoom"],0.5,13,1.2]`）をそのまま使う方針とした（ユーザー確認済み。密度が疎らな分、主曲線と同じ細さでも十分視認でき、z11以降の密な表示と質感が連続する）。

### 2026-07-18 追記: 等高線以外のVBMレイヤーもズーム階層を整理

等高線のズーム分割（上記2件）を実装した結果、逆に「等高線は消えても水涯線・地図記号・注記は無条件でz5から残り続ける」という一貫性の欠如が目立つようになった（ユーザー指摘）。これまで `minzoom` 制限があったのは等高線・主曲線・道路・建物のみで、それ以外の全レイヤー（水涯線、水部構造物、点記号、三角点・水準点、鉄道、送電線、注記等）は無制限（z5から表示）のままだった。

**参考にした設計**: `stars.optgeo.org/bvmap`（国土地理院最適化ベクトルタイル）の実際のTileJSON（`vector_layers[].minzoom`、GUIのstyle定義ではなくタイル自体の実測値）を取得し、レイヤー種別ごとのズーム階層を確認した:

| bvmapレイヤー | minzoom | 備考 |
|---|---|---|
| WA(水域塗り)・AdmArea(行政区画)・RdCL(道路)・RailCL(鉄道)・Cstline(海岸線) | 4 | 基盤スケルトン |
| WRltLine(水部表記記号) | 5 | |
| WStrA(水部構造物面) | 8 | |
| Cntr(等高線) | 9 | |
| RvrCL(河川中心線)・TpgphArea(地形表記面) | 10 | |
| SpcfArea(特定地区界) | 11 | |
| BldA(建物)・Isbt(等深線)・PwrTrnsmL(送電線)・StrctArea/L(構造物)・WStrL(水部構造物線) | 14 | |
| RdCompt/RdEdg(道路構成)・**WL(水涯線＝輪郭線)**・RailTrCL(軌道) | 16 | |

特に重要なのは **WA（水域の塗り）はz4からだが、WL（水涯線という輪郭線）はz16からと大きく分離されている**点。塗りがあれば低ズームでは輪郭線を独立して描く必要が無いという設計思想であり、VBMが水域塗り(5112)はz5からで良いのに水涯線(5101/5102/5106、8火山合計32,000本超)まで無条件にz5から出していたのが、指摘された不整合の直接的な原因と判明した。

**VBMへの適用（bvmapと完全一致はさせず、VBM自身のズーム帯 z5-z14 に合わせて4階層に再構成）**:

| 階層 | minzoom | 分類コード | 該当bvmap概念 |
|---|---|---|---|
| A（据え置き） | 5（無制限） | 5112水域塗り、1102/1103行政界、6201区域界 | WA・AdmArea相当。低密度で基盤スケルトンとして妥当 |
| B | 8 | 5200番台水部構造物、5265砂防ダム | WStrA(z8)に対応 |
| C | 9 or 11 | 7101/7105計曲線（既存、標高100mおきはz9・それ以外z11） | Cntr(z9)に対応 |
| D | 11 | 5101/5102/5106水涯線、6210-6299点記号（噴火口・温泉・ヘリ・避難所等）、7300-7399三角点水準点、8100-8199注記、4257観測施設、4265送電線、2300-2399鉄道 | WL(z16)・SpcfArea(z11)等の「詳細情報」層に相当。bvmapほど遅くはしていない（VBM自体がz14までしかないため） |
| E（既存据え置き） | 13 | 7102/7106主曲線、7131-7135等深線・水深値点、2101-2107道路、3001-3004建物 | BldA(z14)相当。VBMでは実測に基づき13で確定済み（変更なし） |

D階層により、z9〜z10は計曲線（疎ら）のみのすっきりした表示になり、z11で計曲線が密になると同時に水涯線・地図記号・三角点・注記もまとめて出現し、z13で道路・建物・主曲線が加わるという、ズームが上がるごとに情報が段階的に積み上がる一貫した見え方になった。

`scripts/build-vbm.sh` は分類コードを個別列挙ではなく範囲判定（例: `$code >= 5200 and $code <= 5299`）で実装し、将来同系統の分類コードが追加されても自動的に正しい階層に入るようにした。実測（8火山×z5-z14×3x3近傍、全ズームで500KB未満、最大は倶多楽z13の349,593 bytes）で安全性を確認済み。

### 2026-07-19 追記: 地物クリック吹き出しの属性を「一般向けキー」に絞る

`docs/app.js` の地物クリック吹き出し（`map.on('click', ...)`）は、従来 `Object.entries(f.properties)` で全属性を無条件表示していた。しかしVBM/VLCMの属性には、コード表を持たない一般利用者には意味不明な数値・コード系（VBMの `出典コード`/`出典レベル`/`分類コード`/`標高`/`水深`/`水深値`/`表示区分`/`三角点標高`/`水準点標高`、VLCMの `ID`/`code`/`code1`〜`code6`）と、そのまま読んで意味が通る人間可読テキスト（VBMの `名称`/`注記`、VLCMの `class1`〜`class6`/`name`）が混在している。前者を既定で隠し、「詳細」チェックボックス（`.dads-checkbox`、パネルと同一コンポーネント）で全属性表示に切り替えられるようにした。

**許可リストの設計**（`docs/app.js` の `VBM_PUBLIC_KEYS`/`VLCM_PUBLIC_KEYS`）:
- VBM（source-layerが分類コードの数値文字列）: `名称`・`注記` のみ
- VLCM（source-layerが `natural`/`artificial`）: `class1`〜`class6`・`name` のみ

VLCMの `class1`〜`class6` は「火山地形→火口・カルデラ→火口・カルデラ地形→火口・カルデラ縁」のような階層的な人間可読ラベルで、`name` は原則その最深の非null値と一致する（実測: `work/vlcm/natural.ndjson` の先頭featureで `class5="火口・カルデラ縁"` = `name`）。VBMでは分類コード自体が数値でコード表無しには意味を持たないため、`名称`/`注記` という属性キー2つに絞る方が、分類コード単位で許可リストを作るより単純で堅牢。

数値かどうかで自動判定する案（型ベースのヒューリスティック）も検討したが、VBMの技術的属性（`出典コード`等）が常に厳密な数値型とは限らず、明示的なキー許可リストの方が確実と判断し採用した。

## Refinement Process（今後の火山データ追加時）

1. 新しい火山の ZIP を `src/` に配置し `just inspect` で概要確認
2. `just build-vlcm` / `just build-vbm` を実行
3. 上記と同じ `jq` コマンドで属性キー・分類コード分布を実測し、本ドキュメントの表と差分がないか確認
4. 差分があれば（未知の分類コードなど）、本ドキュメントと `scripts/build-vbm.sh` を更新

## References

- MapLibre properties and feature state: https://maplibre.org/maplibre-style-spec/expressions/
- GeoJSON Feature Properties: https://tools.ietf.org/html/rfc7946#section-3.2
